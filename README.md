# steam2gether-server

Two routes are currently available :

`/user/:id` => Where id is the user's steam id. Returns data about the queried user

`/games/:id` => Returns an array containing all of the queried user's games
